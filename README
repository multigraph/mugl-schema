This is the git repository for the mugl-schema project, which contains the MUGL schema definition file, 'mugl.xsd'.

This is the master branch, which currently has nothing on it.  Run 'git branch -a' to see a list of available branches.
